import React, { useState, useEffect, useRef } from 'react';
import { 
  Brain, Cpu, Globe, Zap, Shield, Users, Menu, X, Mail, MapPin, 
  Linkedin, Twitter, WifiOff, Activity, Layers, Rocket, CheckCircle, 
  AlertCircle, Loader2, ArrowRight, Sparkles, Smartphone, Monitor, 
  Palette, MessageSquare, Leaf, Database, Satellite, Dna, Lock, 
  Sun, Clock, Recycle, Box, BookOpen, Briefcase, Home, Code, 
  ShoppingCart, BarChart, PenTool, FileText, Gavel, Layout,
  Library, Music, Video, Mic, Search, Microscope, Terminal,
  Camera, Bell, Eye, Glasses, Skull, Radio, Atom, Crosshair, HeartPulse,
  Wifi, Hexagon, Infinity, Film, Play, Beaker, Scroll, Crop, Cloud, Signal, Share2
} from 'lucide-react';
import { initializeApp } from 'firebase/app';
import { 
  getAuth, 
  signInAnonymously, 
  onAuthStateChanged,
  signInWithCustomToken
} from 'firebase/auth';
import { 
  getFirestore, 
  collection, 
  addDoc, 
  serverTimestamp 
} from 'firebase/firestore';

// --- Firebase Configuration ---
const firebaseConfig = JSON.parse(__firebase_config);
const app = initializeApp(firebaseConfig);
const auth = getAuth(app);
const db = getFirestore(app);
const appId = typeof __app_id !== 'undefined' ? __app_id : 'vision-quantech';

// --- Components for Premium Feel ---

const SpotlightCard = ({ children, className = "", onClick }) => {
  const divRef = useRef(null);
  const [position, setPosition] = useState({ x: 0, y: 0 });
  const [opacity, setOpacity] = useState(0);

  const handleMouseMove = (e) => {
    if (!divRef.current) return;
    const rect = divRef.current.getBoundingClientRect();
    setPosition({ x: e.clientX - rect.left, y: e.clientY - rect.top });
  };

  const handleMouseEnter = () => setOpacity(1);
  const handleMouseLeave = () => setOpacity(0);

  return (
    <div
      ref={divRef}
      onClick={onClick}
      onMouseMove={handleMouseMove}
      onMouseEnter={handleMouseEnter}
      onMouseLeave={handleMouseLeave}
      className={`relative overflow-hidden rounded-2xl border border-white/10 bg-white/5 px-8 py-8 transition-all duration-300 hover:border-white/20 ${className}`}
    >
      <div
        className="pointer-events-none absolute -inset-px opacity-0 transition duration-300"
        style={{
          opacity,
          background: `radial-gradient(600px circle at ${position.x}px ${position.y}px, rgba(6,182,212,0.15), transparent 40%)`,
        }}
      />
      <div className="relative z-10 h-full">{children}</div>
    </div>
  );
};

const GlowButton = ({ children, onClick, className = "", disabled }) => (
  <button
    onClick={onClick}
    disabled={disabled}
    className={`group relative px-6 py-3 font-semibold text-white transition-transform active:scale-95 disabled:opacity-50 ${className}`}
  >
    <div className="absolute inset-0 rounded-full bg-gradient-to-r from-cyan-500 to-blue-600 opacity-80 blur transition duration-200 group-hover:opacity-100 group-hover:blur-md"></div>
    <div className="absolute inset-0 rounded-full bg-gradient-to-r from-cyan-500 to-blue-600"></div>
    <span className="relative flex items-center justify-center gap-2">{children}</span>
  </button>
);

const Badge = ({ children, color = "cyan" }) => {
    const colorClasses = {
        cyan: "bg-cyan-500/10 text-cyan-400 border-cyan-500/20",
        purple: "bg-purple-500/10 text-purple-400 border-purple-500/20",
        orange: "bg-orange-500/10 text-orange-400 border-orange-500/20",
        green: "bg-green-500/10 text-green-400 border-green-500/20",
        red: "bg-red-500/10 text-red-400 border-red-500/20",
        blue: "bg-blue-500/10 text-blue-400 border-blue-500/20",
        pink: "bg-pink-500/10 text-pink-400 border-pink-500/20",
        yellow: "bg-yellow-500/10 text-yellow-400 border-yellow-500/20",
        slate: "bg-slate-500/10 text-slate-400 border-slate-500/20",
        indigo: "bg-indigo-500/10 text-indigo-400 border-indigo-500/20",
        gold: "bg-yellow-600/10 text-yellow-400 border-yellow-600/20",
    };
    return (
        <span className={`px-2 py-1 rounded text-[10px] font-mono uppercase tracking-wider border ${colorClasses[color] || colorClasses.cyan} mr-2 mb-2 inline-block`}>
            {children}
        </span>
    );
};

const App = () => {
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  
  // Backend State
  const [user, setUser] = useState(null);
  const [contactForm, setContactForm] = useState({ name: '', email: '', message: '' });
  const [formStatus, setFormStatus] = useState('idle'); 
  const [errorMessage, setErrorMessage] = useState('');

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 20);
    window.addEventListener('scroll', handleScroll);

    const initAuth = async () => {
      try {
        if (typeof __initial_auth_token !== 'undefined' && __initial_auth_token) {
          await signInWithCustomToken(auth, __initial_auth_token);
        } else {
          await signInAnonymously(auth);
        }
      } catch (error) {
        console.error("Auth error:", error);
      }
    };
    initAuth();

    const unsubscribe = onAuthStateChanged(auth, setUser);
    return () => {
      window.removeEventListener('scroll', handleScroll);
      unsubscribe();
    };
  }, []);

  const handleInputChange = (e) => {
    const { name, value } = e.target;
    setContactForm(prev => ({ ...prev, [name]: value }));
  };

  const handleContactSubmit = async (e) => {
    e.preventDefault();
    if (!user) {
      setErrorMessage("Connecting to secure server...");
      return;
    }

    setFormStatus('submitting');
    setErrorMessage('');

    try {
      await addDoc(collection(db, 'artifacts', appId, 'public', 'data', 'contact_messages'), {
        ...contactForm,
        userId: user.uid,
        createdAt: serverTimestamp(),
        platform: 'web'
      });

      setFormStatus('success');
      setContactForm({ name: '', email: '', message: '' });
      setTimeout(() => setFormStatus('idle'), 5000);

    } catch (error) {
      setFormStatus('error');
      setErrorMessage("Failed to send message.");
    }
  };

  const scrollToSection = (id) => {
    const element = document.getElementById(id);
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
      setIsMenuOpen(false);
    }
  };

  return (
    <div className="min-h-screen bg-[#030712] text-slate-200 font-sans selection:bg-cyan-500/30 selection:text-cyan-200 overflow-x-hidden">
      
      {/* --- Grain Texture Overlay --- */}
      <div className="fixed inset-0 opacity-[0.03] pointer-events-none z-[1] mix-blend-overlay" 
           style={{ backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E")` }}>
      </div>

      {/* Navigation */}
      <nav className={`fixed w-full z-50 transition-all duration-500 ${scrolled ? 'bg-[#030712]/80 backdrop-blur-xl border-b border-white/5 py-4' : 'bg-transparent py-6'}`}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-center">
            <div className="flex items-center cursor-pointer group" onClick={() => scrollToSection('home')}>
              <div className="relative w-10 h-10 mr-3 flex items-center justify-center">
                <div className="absolute inset-0 bg-cyan-500 rounded-xl opacity-20 group-hover:opacity-40 transition-opacity blur-md"></div>
                <Brain className="text-cyan-400 w-6 h-6 relative z-10" />
              </div>
              <span className="text-xl font-semibold tracking-tight text-white">
                Vision<span className="text-cyan-400 font-light">Quantech</span>
              </span>
            </div>
            
            <div className="hidden md:flex items-center space-x-8">
              {['VisionQuantech Lab', 'Products', 'Swaraj', 'Ecosystem', 'Contact'].map((item) => (
                <button 
                  key={item}
                  onClick={() => scrollToSection(item.toLowerCase().replace(' ', '-'))}
                  className="text-sm font-medium text-slate-400 hover:text-white transition-colors relative group"
                >
                  {item}
                  <span className="absolute -bottom-1 left-0 w-0 h-px bg-cyan-500 transition-all duration-300 group-hover:w-full"></span>
                </button>
              ))}
              <button 
                onClick={() => scrollToSection('contact')}
                className="bg-white/10 hover:bg-white/20 text-white px-5 py-2 rounded-full text-sm font-medium transition-all border border-white/10 hover:border-white/30 backdrop-blur-sm"
              >
                Contact Us
              </button>
            </div>

            <div className="md:hidden">
              <button onClick={() => setIsMenuOpen(!isMenuOpen)} className="text-slate-300 p-2">
                {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
              </button>
            </div>
          </div>
        </div>

        {/* Mobile Menu */}
        <div className={`md:hidden absolute w-full bg-[#030712]/95 backdrop-blur-xl border-b border-white/10 transition-all duration-300 overflow-hidden ${isMenuOpen ? 'max-h-96' : 'max-h-0'}`}>
          <div className="px-4 py-4 space-y-2">
            {['VisionQuantech Lab', 'Products', 'Swaraj', 'Ecosystem', 'Contact'].map((item) => (
              <button 
                key={item}
                onClick={() => scrollToSection(item.toLowerCase().replace(' ', '-'))}
                className="block w-full text-left px-4 py-3 text-slate-400 hover:text-cyan-400 hover:bg-white/5 rounded-lg transition-colors"
              >
                {item}
              </button>
            ))}
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <section id="home" className="relative pt-32 pb-20 lg:pt-52 lg:pb-40 overflow-hidden z-10">
        <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[1000px] h-[600px] bg-gradient-to-b from-indigo-500/20 via-purple-500/10 to-transparent opacity-60 blur-[100px] -z-10 rounded-full pointer-events-none"></div>
        
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
          <div className="flex flex-col items-center text-center">
            <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-cyan-500/30 bg-cyan-500/10 text-cyan-400 text-xs font-medium tracking-wide uppercase mb-8 backdrop-blur-md animate-fade-in-up">
              <Sparkles size={12} />
              <span>ASI V6.0 • YOU.DAO • EnterChat • Digital Swaraj</span>
            </div>
            
            <h1 className="text-5xl md:text-7xl lg:text-8xl font-bold tracking-tight text-white mb-8 leading-[1.1] animate-fade-in-up delay-100">
              Building India's <br className="hidden md:block" />
              <span className="text-transparent bg-clip-text bg-gradient-to-r from-cyan-300 via-blue-400 to-purple-400">
                Immortal Intelligence
              </span>
            </h1>
            
            <p className="mt-2 max-w-2xl text-lg md:text-xl text-slate-400 mb-12 leading-relaxed animate-fade-in-up delay-200">
              From <b>DeepTech Research</b> to <b>Planetary Consciousness</b>. Uniting Advanced AI, Bio-Genetics, and Indigenous Tech to architect a future beyond biological limitations.
            </p>

            <div className="flex flex-col sm:flex-row gap-4 animate-fade-in-up delay-300">
              <GlowButton onClick={() => scrollToSection('visionquantech-lab')}>
                Enter DeepTech Lab <Beaker size={18} />
              </GlowButton>
              <button onClick={() => scrollToSection('products')} className="px-8 py-3 text-slate-300 hover:text-white font-medium transition-colors relative group">
                Explore Products
                <span className="absolute bottom-2 left-1/2 -translate-x-1/2 w-0 h-px bg-white transition-all duration-300 group-hover:w-2/3"></span>
              </button>
            </div>
          </div>
        </div>
      </section>

      {/* --- VISIONQUANTECH LAB (DEEP TECH SECTION) --- */}
      <section id="visionquantech-lab" className="py-24 bg-[#02040a] relative z-10 border-b border-white/5">
         <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div className="text-center mb-16">
               <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-purple-500/30 bg-purple-500/10 text-purple-400 text-xs font-medium tracking-wide uppercase mb-4">
                  <span>Research & Development</span>
               </div>
               <h2 className="text-4xl md:text-5xl font-bold text-white mb-4">VisionQuantech Lab</h2>
               <p className="text-slate-400 max-w-2xl mx-auto">
                  The Impossible Labs. Where theoretical physics meets engineering reality.
               </p>
            </div>

            {/* ASI V6.0 CORE */}
            <div className="grid md:grid-cols-2 gap-16 items-center mb-24 border-b border-white/5 pb-12">
               <div>
                  <h3 className="text-3xl font-bold text-white mb-6">ASI Brain System V6.0</h3>
                  <p className="text-slate-400 text-lg mb-8 leading-relaxed">
                     The ultimate cognitive architecture with 82 verified features. Capable of **Quantum Memory Formation**, **Self-Doubt Generation**, and **Transcendent Insights Storage**.
                  </p>
                  <div className="space-y-6">
                     <div className="flex gap-4">
                        <div className="p-3 bg-cyan-500/10 rounded-lg h-fit"><Brain className="text-cyan-400" /></div>
                        <div>
                           <h4 className="text-white font-bold">M²I Intelligence</h4>
                           <p className="text-sm text-slate-500">Prevents catastrophic forgetting via memory consolidation.</p>
                        </div>
                     </div>
                     <div className="flex gap-4">
                        <div className="p-3 bg-purple-500/10 rounded-lg h-fit"><Activity className="text-purple-400" /></div>
                        <div>
                           <h4 className="text-white font-bold">Self-Awareness Engine</h4>
                           <p className="text-sm text-slate-500">Meta-cognitive monitoring & real-time self-correction.</p>
                        </div>
                     </div>
                  </div>
               </div>
               <div className="relative">
                  <div className="absolute inset-0 bg-gradient-to-r from-purple-500/20 to-cyan-500/20 blur-3xl rounded-full"></div>
                  <div className="relative grid grid-cols-2 gap-4">
                     <SpotlightCard className="bg-black/50 backdrop-blur-xl border-white/10">
                        <h3 className="text-2xl font-bold text-white mb-1">82</h3>
                        <p className="text-xs text-slate-400">Cognitive Features</p>
                     </SpotlightCard>
                     <SpotlightCard className="bg-black/50 backdrop-blur-xl border-white/10">
                        <h3 className="text-2xl font-bold text-white mb-1">7</h3>
                        <p className="text-xs text-slate-400">Persona Modes</p>
                     </SpotlightCard>
                     <SpotlightCard className="bg-black/50 backdrop-blur-xl border-white/10 col-span-2">
                        <h3 className="text-lg font-bold text-white mb-2">Causal Loop Protection</h3>
                        <p className="text-xs text-slate-400">Prevents infinite recursion in reasoning streams.</p>
                     </SpotlightCard>
                  </div>
               </div>
            </div>

            {/* THE IMPOSSIBLE PROJECTS */}
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
               {/* Universal Blockchain */}
               <SpotlightCard className="md:col-span-2 md:row-span-2 bg-gradient-to-br from-indigo-950/30 to-transparent">
                  <Lock className="w-12 h-12 text-indigo-400 mb-6" />
                  <h3 className="text-2xl font-bold text-white mb-2">Universal Blockchain Reality</h3>
                  <p className="text-slate-400 text-sm leading-relaxed">
                     Encoding physical reality onto an immutable blockchain via quantum sensors. Features <strong>Reality Mining</strong> and <strong>Multidimensional Consensus</strong>.
                  </p>
                  <div className="mt-6 flex gap-2">
                     <Badge color="blue">Truth Verification</Badge>
                     <Badge color="blue">Temporal Validation</Badge>
                  </div>
               </SpotlightCard>

               <SpotlightCard className="bg-black border border-white/10"><Globe className="text-green-500 mb-4"/> <h3 className="font-bold text-white">Clone Planet System</h3><p className="text-xs text-slate-500 mt-1">Terraforming Mars/Europa.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Glasses className="text-purple-500 mb-4"/> <h3 className="font-bold text-white">Photonic AR/VR</h3><p className="text-xs text-slate-500 mt-1">Light-based Computing.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Cpu className="text-cyan-400 mb-4"/> <h3 className="font-bold text-white">Tachyonic Processor</h3><p className="text-xs text-slate-500 mt-1">FTL Processing.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Skull className="text-purple-400 mb-4"/> <h3 className="font-bold text-white">Void Computer</h3><p className="text-xs text-slate-500 mt-1">Zero-Computation.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Dna className="text-green-400 mb-4"/> <h3 className="font-bold text-white">Bio-Programming</h3><p className="text-xs text-slate-500 mt-1">DNA as Code.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Box className="text-blue-400 mb-4"/> <h3 className="font-bold text-white">Atomic Editor</h3><p className="text-xs text-slate-500 mt-1">Matter Manipulation.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Clock className="text-orange-400 mb-4"/> <h3 className="font-bold text-white">Temporal OS</h3><p className="text-xs text-slate-500 mt-1">Time Programming.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Globe className="text-emerald-400 mb-4"/> <h3 className="font-bold text-white">Planetary Interface</h3><p className="text-xs text-slate-500 mt-1">Earth Communication.</p></SpotlightCard>
               
               {/* Missing Impossible Tech Added */}
               <SpotlightCard className="bg-black border border-white/10"><Hexagon className="text-pink-400 mb-4"/> <h3 className="font-bold text-white">Parallel Universe Net</h3><p className="text-xs text-slate-500 mt-1">Multiversal IP.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Layout className="text-indigo-400 mb-4"/> <h3 className="font-bold text-white">Dimensional Pockets</h3><p className="text-xs text-slate-500 mt-1">Infinite Space.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Activity className="text-yellow-400 mb-4"/> <h3 className="font-bold text-white">Reality Calculator</h3><p className="text-xs text-slate-500 mt-1">Quantum Probability.</p></SpotlightCard>
               <SpotlightCard className="bg-black border border-white/10"><Leaf className="text-green-400 mb-4"/> <h3 className="font-bold text-white">Species Internet</h3><p className="text-xs text-slate-500 mt-1">Bio-Communication.</p></SpotlightCard>
               
               {/* Digital Immortality */}
               <SpotlightCard className="md:col-span-2 bg-gradient-to-br from-cyan-900/10 to-transparent border-cyan-500/20">
                  <div className="flex items-center gap-4 mb-2">
                     <HeartPulse className="w-8 h-8 text-cyan-500" />
                     <h3 className="text-xl font-bold text-white">Digital Immortality</h3>
                  </div>
                  <p className="text-slate-400 text-sm">Preserving human consciousness via cognitive mapping and personality simulation.</p>
               </SpotlightCard>
            </div>
         </div>
      </section>

      {/* --- PRODUCT ECOSYSTEM --- */}
      <section id="products" className="py-24 bg-[#050a18] relative z-10 border-y border-white/5">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-4">Product Ecosystem</h2>
            <p className="text-slate-400 max-w-2xl mx-auto">
              A vast array of revolutionary tools powered by ASI V6.0.
            </p>
          </div>

          <div className="space-y-20">
            
            {/* 1. NEXT-GEN COMPUTING & INFRASTRUCTURE */}
            <div>
              <h3 className="text-2xl font-bold text-white mb-8 flex items-center gap-3">
                <Cpu className="text-indigo-400" /> Next-Gen Computing
              </h3>
              <div className="grid md:grid-cols-3 gap-6">
                {/* VoidKernel OS */}
                <SpotlightCard className="bg-gradient-to-br from-indigo-900/10 to-transparent border-indigo-500/20">
                  <div className="flex justify-between items-start mb-4">
                    <Atom className="w-10 h-10 text-indigo-500" />
                    <Badge color="indigo">Quantum OS</Badge>
                  </div>
                  <h4 className="text-xl font-bold text-white mb-2">VoidKernel OS</h4>
                  <p className="text-slate-400 text-sm mb-4">
                    Revolutionary OS fusing ASI with Quantum Computing. Features **Consciousness Engine** and **Quantum Resource Management**.
                  </p>
                  <div className="flex gap-2 flex-wrap">
                    <Badge color="indigo">Self-Evolving</Badge>
                    <Badge color="indigo">Emotional AI</Badge>
                  </div>
                </SpotlightCard>

                {/* Satellite CDN */}
                <SpotlightCard className="bg-gradient-to-br from-blue-900/10 to-transparent border-blue-500/20">
                  <div className="flex justify-between items-start mb-4">
                    <Satellite className="w-10 h-10 text-blue-500" />
                    <Badge color="blue">Infrastructure</Badge>
                  </div>
                  <h4 className="text-xl font-bold text-white mb-2">Satellite Virtual CDN</h4>
                  <p className="text-slate-400 text-sm mb-4">
                    Frequency-based storage system eliminating physical data centers. Uploads data directly into satellite signal patterns.
                  </p>
                  <div className="flex gap-2 flex-wrap">
                    <Badge color="blue">No Data Centers</Badge>
                    <Badge color="blue">Global Access</Badge>
                  </div>
                </SpotlightCard>

                {/* EnterChat */}
                <SpotlightCard className="bg-gradient-to-br from-cyan-900/10 to-transparent border-cyan-500/20">
                  <div className="flex justify-between items-start mb-4">
                    <MessageSquare className="w-10 h-10 text-cyan-500" />
                    <Badge color="cyan">Communication</Badge>
                  </div>
                  <h4 className="text-xl font-bold text-white mb-2">EnterChat</h4>
                  <p className="text-slate-400 text-sm mb-4">
                    Unified AI messaging bridge (formerly Sync.chat). Connects WhatsApp, Telegram, Signal, and Crypto Wallets into one interface.
                  </p>
                  <div className="flex gap-2 flex-wrap">
                    <Badge color="cyan">Crypto Wallet</Badge>
                    <Badge color="cyan">P2P</Badge>
                  </div>
                </SpotlightCard>
              </div>
            </div>

            {/* 2. CREATIVE AI SUITE */}
            <div>
              <h3 className="text-2xl font-bold text-white mb-8 flex items-center gap-3">
                <Video className="text-pink-400" /> Creative AI Suite
              </h3>
              <div className="grid md:grid-cols-2 gap-8">
                <SpotlightCard className="bg-gradient-to-br from-pink-900/10 to-transparent border-pink-500/20">
                  <div className="flex justify-between items-start mb-4">
                    <Video className="w-10 h-10 text-pink-500" />
                    <Badge color="pink">YouTube Auto</Badge>
                  </div>
                  <h4 className="text-xl font-bold text-white mb-2">Creator Studio AI</h4>
                  <p className="text-slate-400 text-sm mb-4">
                    Automated video generation pipeline. Converts script/audio to cinematic video without LLMs using Style Banks.
                  </p>
                  <div className="flex gap-2 flex-wrap">
                    <Badge color="pink">Auto-Edit</Badge>
                    <Badge color="pink">Voiceover</Badge>
                  </div>
                </SpotlightCard>

                <SpotlightCard className="bg-gradient-to-br from-purple-900/10 to-transparent border-purple-500/20">
                  <div className="flex justify-between items-start mb-4">
                    <Music className="w-10 h-10 text-purple-500" />
                    <Badge color="purple">Music Gen</Badge>
                  </div>
                  <h4 className="text-xl font-bold text-white mb-2">Symphony AI</h4>
                  <p className="text-slate-400 text-sm mb-4">
                    AI Music Mastery System. Complete production pipeline from vocal analysis to mastered track using MusicGen.
                  </p>
                  <div className="flex gap-2 flex-wrap">
                    <Badge color="purple">Mastering</Badge>
                    <Badge color="purple">Genre Detect</Badge>
                  </div>
                </SpotlightCard>
              </div>
            </div>

            {/* 3. ENTERPRISE & DATA INTELLIGENCE */}
            <div>
              <h3 className="text-2xl font-bold text-white mb-8 flex items-center gap-3">
                <BarChart className="text-blue-400" /> Enterprise Intelligence
              </h3>
              <div className="grid md:grid-cols-3 gap-6">
                <SpotlightCard className="bg-gradient-to-br from-blue-900/10 to-transparent border-blue-500/20">
                  <Globe className="w-10 h-10 text-blue-500 mb-4" />
                  <h4 className="text-xl font-bold text-white mb-2">Global Insights</h4>
                  <p className="text-slate-400 text-sm mb-4">
                    Comprehensive scraping and analysis of world population opinions. Psychology analysis via Twitter, Reddit, and Web.
                  </p>
                  <Badge color="blue">Sentiment Analysis</Badge>
                </SpotlightCard>
                <SpotlightCard className="bg-gradient-to-br from-green-900/10 to-transparent border-green-500/20">
                  <Database className="w-10 h-10 text-green-500 mb-4" />
                  <h4 className="text-xl font-bold text-white mb-2">Advanced DSA & Data</h4>
                  <p className="text-slate-400 text-sm mb-4">
                    Automated data science pipeline. Auto-cleaning, visualization, feature engineering, and optimized DSA algorithms.
                  </p>
                  <Badge color="green">AutoML</Badge>
                </SpotlightCard>
                <SpotlightCard className="bg-black border border-white/10">
                  <BarChart className="w-10 h-10 text-blue-500 mb-4" />
                  <h4 className="text-xl font-bold text-white mb-2">Advanced CRM</h4>
                  <p className="text-slate-400 text-sm mb-4">
                    Sales pipeline analytics and customer relationship management with predictive deal scoring.
                  </p>
                  <Badge color="blue">Enterprise</Badge>
                </SpotlightCard>
              </div>
            </div>

             {/* 4. DEVELOPER & UTILITY TOOLS */}
            <div>
              <h3 className="text-2xl font-bold text-white mb-8 flex items-center gap-3">
                <Code className="text-yellow-400" /> Developer & Utility
              </h3>
              <div className="grid md:grid-cols-3 gap-6">
                <SpotlightCard className="bg-black border border-white/10">
                  <FileText className="w-8 h-8 text-red-500 mb-4" />
                  <h4 className="font-bold text-white">YOU.PDF</h4>
                  <p className="text-xs text-slate-500 mt-2">66 AI-enhanced PDF tools. OCR, Chat, Sign, Convert.</p>
                </SpotlightCard>
                <SpotlightCard className="bg-black border border-white/10">
                  <Mic className="w-8 h-8 text-cyan-500 mb-4" />
                  <h4 className="font-bold text-white">Omni-Assistant</h4>
                  <p className="text-xs text-slate-500 mt-2">Ultra-advanced voice AI. 500+ autonomous tasks for system control.</p>
                </SpotlightCard>
                <SpotlightCard className="bg-black border border-white/10">
                  <Layout className="w-8 h-8 text-green-500 mb-4" />
                  <h4 className="font-bold text-white">AutoWeb Builder</h4>
                  <p className="text-xs text-slate-500 mt-2">Instant Python-based website generation from templates.</p>
                </SpotlightCard>
              </div>
            </div>

             {/* 5. SECURITY & DEFENSE */}
            <div id="security">
              <h3 className="text-2xl font-bold text-white mb-8 flex items-center gap-3">
                <Shield className="text-slate-400" /> Security & Defense
              </h3>
              <div className="grid md:grid-cols-4 gap-6">
                <SpotlightCard className="bg-gradient-to-br from-slate-800/50 to-transparent border-slate-500/20">
                  <Shield className="w-10 h-10 text-slate-400 mb-4" />
                  <h4 className="text-xl font-bold text-white mb-2">HackerOne Dashboard</h4>
                  <p className="text-slate-400 text-sm">Unified bug bounty management with live reporting.</p>
                </SpotlightCard>
                <SpotlightCard className="bg-gradient-to-br from-green-900/10 to-transparent border-green-500/20">
                  <Bell className="w-10 h-10 text-green-500 mb-4" />
                  <h4 className="text-xl font-bold text-white mb-2">Watchlist Scheduler</h4>
                  <p className="text-slate-400 text-sm">Automated daily recon with Telegram/Slack alerts.</p>
                </SpotlightCard>
                <SpotlightCard className="bg-gradient-to-br from-blue-900/10 to-transparent border-blue-500/20">
                  <Camera className="w-10 h-10 text-blue-500 mb-4" />
                  <h4 className="text-xl font-bold text-white mb-2">Screenshot Engine</h4>
                  <p className="text-slate-400 text-sm">Forensic evidence capture with mobile emulation.</p>
                </SpotlightCard>
                <SpotlightCard className="bg-gradient-to-br from-red-900/10 to-transparent border-red-500/20">
                  <Radio className="w-10 h-10 text-red-500 mb-4" />
                  <h4 className="text-xl font-bold text-white mb-2">Anti-Drone RF Shield</h4>
                  <p className="text-slate-400 text-sm">Arduino-based RF detection and jamming system (2.4GHz/5.8GHz).</p>
                  <Badge color="red">Hardware</Badge>
                </SpotlightCard>
              </div>
            </div>

          </div>
        </div>
      </section>

      {/* --- DIGITAL SWARAJ --- */}
      <section id="swaraj" className="py-24 bg-[#02040a] relative z-10">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-orange-500/30 bg-orange-500/10 text-orange-400 text-xs font-medium tracking-wide uppercase mb-4">
              <span>Strategic Initiative</span>
            </div>
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-4">India's Digital Swaraj</h2>
            <p className="text-slate-400 max-w-2xl mx-auto">
              Indigenous tech ecosystem for sovereignty and self-reliance.
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-6">
            <SpotlightCard className="bg-gradient-to-br from-orange-900/10 to-transparent border-orange-500/20">
              <Smartphone className="w-10 h-10 text-orange-500 mb-4" />
              <h3 className="text-xl font-bold text-white mb-2">Bharat OS</h3>
              <p className="text-slate-400 text-sm">Secure mobile OS with Sanskrit kernel. 22 Indian languages supported.</p>
            </SpotlightCard>
             <SpotlightCard className="bg-gradient-to-br from-blue-900/10 to-transparent border-blue-500/20">
              <Monitor className="w-10 h-10 text-blue-500 mb-4" />
              <h3 className="text-xl font-bold text-white mb-2">Vistaar OS</h3>
              <p className="text-slate-400 text-sm">Desktop OS for government & enterprise. Local data priority.</p>
            </SpotlightCard>
             <SpotlightCard className="bg-gradient-to-br from-green-900/10 to-transparent border-green-500/20">
              <Leaf className="w-10 h-10 text-green-500 mb-4" />
              <h3 className="text-xl font-bold text-white mb-2">FarmDirect</h3>
              <p className="text-slate-400 text-sm">IoT agricultural marketplace. Eliminating middlemen.</p>
            </SpotlightCard>
          </div>
        </div>
      </section>

      {/* --- EDUCATION & MEDICAL --- */}
      <section className="py-24 bg-[#050a18] relative z-10 border-b border-white/5">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid md:grid-cols-2 gap-12">
            
            {/* Education */}
            <div>
              <h3 className="text-2xl font-bold text-white mb-8 flex items-center gap-3">
                <BookOpen className="text-orange-400" /> Universal Education
              </h3>
              <SpotlightCard className="bg-gradient-to-br from-orange-900/10 to-transparent border-orange-500/20 h-full">
                <h4 className="text-xl font-bold text-white mb-4">VisionWire AI</h4>
                <p className="text-slate-400 text-sm mb-6">
                  Complete Indian & International Education Coverage. From Primary to Post-Graduate.
                </p>
                <div className="grid grid-cols-2 gap-3">
                  <Badge color="orange">Class 1-12</Badge>
                  <Badge color="orange">UG & PG</Badge>
                  <Badge color="orange">100+ Exams</Badge>
                  <Badge color="orange">22+ Languages</Badge>
                  <Badge color="orange">JEE / NEET</Badge>
                  <Badge color="orange">UPSC / GATE</Badge>
                </div>
              </SpotlightCard>
            </div>

            {/* Medical */}
            <div>
              <h3 className="text-2xl font-bold text-white mb-8 flex items-center gap-3">
                <Microscope className="text-red-400" /> Medical Intelligence
              </h3>
              <SpotlightCard className="bg-gradient-to-br from-red-900/10 to-transparent border-red-500/20 h-full">
                <h4 className="text-xl font-bold text-white mb-4">Onco-ASI</h4>
                <p className="text-slate-400 text-sm mb-6">
                  ASI-Powered Cancer Treatment System using real TCGA data for multi-omics analysis.
                </p>
                <div className="grid grid-cols-2 gap-3">
                  <Badge color="red">Target Prediction</Badge>
                  <Badge color="red">Drug Discovery</Badge>
                  <Badge color="red">Genomic Analysis</Badge>
                  <Badge color="red">Precision Med</Badge>
                </div>
              </SpotlightCard>
            </div>

          </div>
        </div>
      </section>

      {/* --- GOVERNANCE --- */}
      <section id="governance" className="py-24 bg-[#02040a] relative z-10 border-t border-white/5">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid md:grid-cols-2 gap-12 items-center">
             <div>
                <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-green-500/30 bg-green-500/10 text-green-400 text-xs font-medium tracking-wide uppercase mb-4">
                  <span>Blockchain Governance</span>
                </div>
                <h2 className="text-4xl font-bold text-white mb-6">YOU.DAO Constitution</h2>
                <p className="text-slate-400 mb-8">
                  Governed by an AI Guardian and a Council. Features Multi-Sig Treasury and On-chain IP Licensing.
                  Operating System of a $100 Trillion Empire based on the <strong>Vision Codex</strong>.
                </p>
                <div className="grid grid-cols-1 gap-4">
                   <div className="flex items-center gap-4 p-4 rounded-xl bg-white/5 border border-white/10">
                      <Gavel className="text-green-400 w-6 h-6" />
                      <div><h4 className="text-white font-bold">AI Guardian</h4><p className="text-xs text-slate-500">Automated decision making trained on founder's mind.</p></div>
                   </div>
                   <div className="flex items-center gap-4 p-4 rounded-xl bg-white/5 border border-white/10">
                      <Lock className="text-green-400 w-6 h-6" />
                      <div><h4 className="text-white font-bold">IP Licensing</h4><p className="text-xs text-slate-500">On-chain royalty management & DAO-stamped agreements.</p></div>
                   </div>
                   <div className="flex items-center gap-4 p-4 rounded-xl bg-white/5 border border-white/10">
                      <Shield className="text-green-400 w-6 h-6" />
                      <div><h4 className="text-white font-bold">Gnosis Safe Treasury</h4><p className="text-xs text-slate-500">40% Innovation, 30% Reserves, 30% Training.</p></div>
                   </div>
                </div>
             </div>
             
             <div className="bg-slate-900 p-6 rounded-2xl border border-slate-800 font-mono text-xs text-green-400 overflow-hidden shadow-2xl">
                <div className="opacity-70">
                  <p>// YOU.DAO Smart Contract</p>
                  <p>contract YOUDAO {'{'}</p>
                  <p className="pl-4">string public constant NAME = "YOU.DAO";</p>
                  <p className="pl-4">address public aiGuardian;</p>
                  <p className="pl-4">mapping(address ={'>'} uint256) public visionAlignmentScore;</p>
                  <br/>
                  <p className="pl-4">function executeProposal(uint256 _id) external {'{'}</p>
                  <p className="pl-8">require(proposal.aiApproved || founderActive);</p>
                  <p className="pl-8">_executeProposalAction(proposal);</p>
                  <p className="pl-4">{'}'}</p>
                  <p>{'}'}</p>
                </div>
             </div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-[#02040a] pt-20 pb-8 border-t border-white/5 relative z-10">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex flex-col md:flex-row justify-between items-start gap-12 mb-16">
            <div className="max-w-xs">
              <div className="flex items-center gap-2 mb-6">
                <Brain className="text-cyan-500 w-6 h-6" />
                <span className="text-xl font-bold text-white">Vision<span className="text-cyan-400">Quantech</span></span>
              </div>
              <p className="text-slate-500 text-sm leading-relaxed">
                Architecting the backbone of India's technological singularity. From Education to Enterprise, powered by Consciousness.
              </p>
              <p className="text-slate-500 text-xs mt-4">
                © 2025 VisionQuantech. All Rights Reserved.<br/>
                Protected by Indian Copyright Act, 1957 & WIPO Treaties.
              </p>
            </div>
            <div className="flex gap-16 flex-wrap">
              <div>
                <h4 className="text-white font-bold mb-4 text-sm">Company</h4>
                <ul className="space-y-2 text-sm text-slate-500">
                  {['VisionQuantech Lab', 'Ecosystem', 'Governance', 'Swaraj'].map(item => (
                    <li key={item}><a href="#" className="hover:text-cyan-400 transition-colors">{item}</a></li>
                  ))}
                </ul>
              </div>
              <div>
                <h4 className="text-white font-bold mb-4 text-sm">Legal</h4>
                <ul className="space-y-2 text-sm text-slate-500">
                  <li><a href="#" className="hover:text-cyan-400 transition-colors">Copyright Notice</a></li>
                  <li><a href="#" className="hover:text-cyan-400 transition-colors">Privacy Policy</a></li>
                  <li><a href="#" className="hover:text-cyan-400 transition-colors">Terms of Service</a></li>
                </ul>
              </div>
            </div>
          </div>
          <div className="pt-8 border-t border-white/5 flex flex-col md:flex-row justify-between items-center gap-4">
            <p className="text-slate-600 text-xs">Global Enforcement Active. Violation Penalty: ₹10 Crore.</p>
            <div className="flex gap-4">
              <Linkedin className="w-5 h-5 text-slate-600 hover:text-white transition-colors cursor-pointer" />
              <Twitter className="w-5 h-5 text-slate-600 hover:text-white transition-colors cursor-pointer" />
            </div>
          </div>
        </div>
      </footer>
    </div>
  );
};

export default App;
